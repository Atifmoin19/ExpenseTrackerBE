"""Pydantic v2 request/response models for the `/api/v1` HTTP boundary — one
module per CONTRACT.md area. These validate incoming request bodies and shape
outgoing response `data` payloads; see utils/responses.py for the envelope."""
